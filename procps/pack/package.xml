<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE control [
  <!ENTITY procps SYSTEM "procps.xml">
  <!ENTITY libproc-dev SYSTEM "libproc-dev.xml">
  <!ENTITY rules SYSTEM "rules.xml">
  <!ENTITY changelog SYSTEM "changelog.xml">
  <!ENTITY patchlist SYSTEM "patchlist.xml">
]>
<control xmlns:xi="http://www.w3.org/2001/XInclude">

    <defines>
        <def name="XPACK_SOURCE_DIR"  value="procps-src"/>
        <def name="XPACK_BUILD_DIR"  value="tmp-build"/>
        <def name="XPACK_INSTALL_DIR" value="tmp-install"/>
    </defines>

    <source name="procps" architecture-independent="false">
        <description>
            <summary>/proc file system utilities</summary>
            <p>
            This package provides command line and full screen utilities for browsing
            procfs, a &quot;pseudo&quot; file system dynamically generated by the kernel to
            provide information about the status of entries in its process table
            (such as whether the process is running, stopped, or a &quot;zombie&quot;).
            </p>
            <p>
            It contains free, kill, pkill, pgrep, pmap, ps, pwdx, skill, slabtop,
            snice, sysctl, tload, top, uptime, vmstat, w, and watch.
            </p>
        </description>

        <sources>
            <file src="procps-3.2.8.tar.gz"/>
        </sources>

        &patchlist;

        <requires>
            <package name="libncurses5-dev"/>
            <package name="libncursesw5-dev"/>
        </requires>

        <!-- build rules -->
        &rules;
    </source>

    <!-- package rules -->
    &procps;
    &libproc-dev;

    <!-- changelog -->
    &changelog;

</control> 
