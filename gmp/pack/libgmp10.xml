<?xml version="1.0" encoding="utf-8"?>
<package name="libgmp10" section="libs">
    <description>
        <summary>Multiprecision arithmetic library (32bit)</summary>
        <p>
        GNU MP is a programmer's library for arbitrary precision
        arithmetic (ie, a bignum package).  It can operate on signed
        integer, rational, and floating point numeric types.  This
        package includes a 32 bit version of the shared library.
        </p>
        <p>
        It has a rich set of functions, and the functions have a regular
        interface.
        </p>
    </description>

    <contents>
        <file src="/usr/lib/libgmp.so.*"/>
        <file src="/usr/lib/libmp.so.*"/>
    </contents>
</package>
